PORT=3000
HOST=0.0.0.0
SC_HTTPS=0
SC_SESSION_HOURS=8

# Crie um arquivo .env local e troque estes valores antes de usar em produção.
SC_ADMIN_USER=admin
SC_ADMIN_PASSWORD=ALTERE_ANTES_DE_USAR

# Opcional:
# SC_DB_PATH=./data/sc-central.sqlite

# Integração opcional via webhook (ERP/PDV/CRM)
SC_WEBHOOK_URL=
SC_WEBHOOK_TOKEN=
